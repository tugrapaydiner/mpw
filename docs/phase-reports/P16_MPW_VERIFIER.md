# P16 MPW verifier centerpiece

objective: powerset-driven global-minimum verification with stable ids, no leaks.
files changed: `src/engine/mpwManifest.ts` (+experimentId home), `src/engine/mpwCounterfactual.ts` (uses it), `src/engine/mpwVerify.ts` (cardinality + id per table row), tests (INCONCLUSIVE target, powerset enumeration, timeout room for heavy files), this report.
tests run: `npm run verify` green (119/119 in 21 files, build ok).
failures discovered: full-suite timeouts under parallel load (not logic) — fixed with a 60s test timeout; two arity/type frictions from the id move — fixed.
fixes: timeout config, 5-arg id calls, index signature on EngineMeta.

coverage: powerset generated by bitmask/combinatorics (never typed); per-row cardinality + id + scores + CI + sufficient + coverage; canonical evaluates all 16 past the early min; general verifier caps at 20 dims; INCONCLUSIVE target verified; enumeration unique + deterministic; no budget/lab special-casing anywhere (grepped).
gate result: GREEN.
blockers: none.
