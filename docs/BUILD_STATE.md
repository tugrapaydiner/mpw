# Research Branch Build State

**Branch:** `research/deep-mpw-overhaul`  
**Base:** `main@78a74cf45f16da19d03eb84e995bc23ea87f11f6`  
**Main modified by this branch:** no  
**Status:** active research overhaul; not merged; not publication-grade.

This file records subsystem state without hard-coding a moving branch HEAD. Resolve the current remote SHA before reporting or reproducing a result.

## Implemented research core

- finite typed protocol schemas with arbitrary categorical scalar values;
- generic directional protocol substitution;
- exact minimum-cardinality and full-landscape search;
- all co-minimum witness recovery;
- separate minimum-proof and landscape-exhaustiveness fields;
- empty-witness, no-witness, inconclusive, omitted-dimension, and invalid-evidence states;
- bidirectional reconciliation;
- effect-restoration, nuisance, and interaction diagnostics;
- black-box query lower-bound formulas and certificate-coverage checks.

## Evaluation evidence

- canonical deterministic 400-item synthetic fixture retained as a tutorial and regression adapter;
- multi-case deterministic reconciliation benchmark with known-ground-truth landscapes;
- exhaustive search model check over every Boolean sufficiency map on three coordinates;
- external fragility-grid study over five frozen candidates;
- qualified external result: five cardinality-two witnesses, zero strict interaction-only cases;
- search-baseline study comparing exact verification with heuristic proposal strategies;
- deterministic robust-witness Monte Carlo implementation study.

## Statistics

- category-stratified paired fixed-item bootstrap;
- paired discordance and exact conditional McNemar/binomial diagnostics;
- synchronized simultaneous bootstrap for a predeclared finite family;
- familywise robust-witness lower bounds for repeated Bernoulli target reproduction;
- explicit separation of deterministic, pointwise, simultaneous, and repeated-run claims.

The canonical fixture does not contain repeated independent model runs and therefore does not establish robust real-world witness membership.

## Portability and provenance

- RFC 8785/JCS canonicalization and SHA-256 identities;
- finalized canonical publication bundles;
- direction- and candidate-bound reconciliation certificate v2;
- separate content-integrity and scientific replay validation;
- standalone certificate verification script;
- static protocol-grid package v1 with complete endpoint-cube validation;
- versioned JSON Schemas for certificates and static grid packages;
- CLI builder and reconciler for static grid packages.

Hashes prove content identity relative to a known digest. They do not authenticate publishers or establish truth.

## Interfaces

- React human investigation interface;
- four semantic WebMCP operations over shared application services;
- strict handler validation and coded errors;
- deterministic trace grading and adversarial tool-contract tests.

Live production-browser behavior and probabilistic multi-agent performance remain separate pending evaluations.

## Verification surfaces

The supported repository gate is `npm run verify`:

```text
typecheck -> lint -> tests -> production build
```

Research workflows additionally exercise the reconciliation benchmark, statistical family analysis, artifact generation, certificate replay, external study, and independent certificate audit.

A workflow configuration or prior green run is not represented as proof for a later commit. Final reporting must identify the exact commit and actual run or clean-clone execution inspected.

## Highest-priority remaining evidence

1. preregistered repeated real model evaluations for robust-witness inference;
2. a broader and representative external-dispute corpus;
3. controlled WebMCP-versus-DOM agent experiments across multiple agents;
4. independent third-party reproduction;
5. publisher authentication and executable/federated package design;
6. structure-aware exact search only where assumptions can be validated.

## Deprecated build-state claims

The earlier P01–P45 phase sequence, video deadline, and “submit/freeze” status are historical hackathon notes. They are not the current research roadmap or verification authority.
