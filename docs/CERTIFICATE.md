# cert (clock-free)

body -> canonical -> sha256 -> certificateHash, id = `mpw-<first 16 hex>`.
no timestamps in body. ui stuff like displayedAt / downloadedAt lives outside, never hashed.
same science twice = same identity.
