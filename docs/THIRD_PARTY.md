# third-party material (checked pre-release)

bundled: none. no images, logos, fonts, music, or datasets. ui uses system fonts + original styling. names are synthetic (MODEL_A/B, Lab A/B).

deps (all permissive, runtime needs none beyond the static bundle):
react MIT, react-dom MIT, vite MIT, vitest MIT, typescript Apache-2.0, eslint MIT, @eslint/js MIT, typescript-eslint MIT, @vitejs/plugin-react MIT, @types/* MIT.

brand names (OpenAI, ChatGPT, Chrome, GitHub) appear only as factual references in docs. no logos, no bundled third-party code beyond the libs above.
